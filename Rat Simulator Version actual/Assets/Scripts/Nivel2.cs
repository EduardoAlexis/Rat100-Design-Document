using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Nivel2 : MonoBehaviour
{
    public static bool Inlvl2;
    // Start is called before the first frame update
    void Start()
    {
        Inlvl2 = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
